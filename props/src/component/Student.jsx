import React, { useState } from 'react'

function Student({name}) {
    //const[students,setStudents]=useState("Sam")
    return (
        <div>
            <h1>Student Name={name}</h1>
            
        </div>
    )
}

export default Student
